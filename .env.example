# S3/MinIO Connection (for development reference only)
# Actual credentials are managed through the UI and encrypted in localStorage
VITE_DEFAULT_ENDPOINT=http://localhost:9000
VITE_DEFAULT_REGION=us-east-1

# Docker Compose MinIO Credentials
# Use these credentials when adding a new connection in the UI
# Username: minioadmin
# Password: minioadmin123
# MinIO Console: http://localhost:9001
# MinIO API: http://localhost:9000
